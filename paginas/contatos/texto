



$dados = $user->buscarDados();
if (count($dados) > 0) {
    for ($i = 0; $i < count($dados); $i++) {
        echo "<tr>";
        foreach ($dados[$i] as $key => $values) {
            if ($key != "idContato") {
                if ($key != "senha"){
                if ($key != "flagFavContato")
                    echo "<td>$values</td>";
                }
            }